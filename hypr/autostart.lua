-- Extra autostart processes.
-- o.launch_on_start("my-service")

-- Workspace overview module (quickshell-overview), toggled with SUPER + GRAVE
o.launch_on_start("qs -c overview")

o.launch_on_start("obs --startreplaybuffer --minimize-to-tray")
